package practice

// ReduceSlice folds nums down to a single value: starting from initial, it
// repeatedly applies reducer(accumulator, currentElement) for each element
// in order, and returns the final accumulator. If nums is empty, return
// initial unchanged.
func ReduceSlice(nums []int, initial int, reducer func(acc, cur int) int) int {
	// TODO: implement
	return 0
}
