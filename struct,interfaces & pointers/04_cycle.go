package practice

// HasCycle reports whether the linked list starting at head contains a
// cycle. Use Floyd's tortoise-and-hare algorithm (two pointers, one moving
// twice as fast as the other) for O(1) space — don't use a map/set of
// visited nodes.
func HasCycle(head *Node) bool {
	// TODO: implement
	return false
}
